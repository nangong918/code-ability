# QiLing



## 基本功能

基于Flutter开发麒麟上位机控制App：
1. 与IJetty服务端通信，实现瓶组控制、拼接设置、连接关系配置等上位机操作。
2. 使用RTMP+X264编码推流，通过Media3 ExoPlayer（rtmp/hls）拉流播放，实时展示LED/LCD大屏幕设备状态与预监板卡音视频流。
3. 使用FFmpeg+RTSP进行文件推流，通过Media3 ExoPlayer（rtsp/hls）拉流播放。


## 问题
一、项目整体与架构
1. 麒麟 Flutter App 在整个系统中承担什么角色？与 IJetty、板卡、大屏之间的数据流是怎样的？
2. 为什么选用 Flutter 开发控制端？流媒体播放是采用 Flutter 组件还是原生 Android 视图？

二、推流与编码（RTMP + X264）
1. 推流端是板卡还是设备本机？X264 编码参数如何配置（I 帧间隔、码率、分辨率、帧率）？
2. 为什么选择 RTMP 推流、X264 编码？有没有考虑 H.265、WebRTC？
3. 嵌入式 RK 平台上，X264 软编码还是硬编码？是否使用 RK 硬件编解码？
4. 推流过程中出现网络抖动、码率波动，如何做缓冲、丢帧、码率自适应？

三、拉流与播放（Media3 ExoPlayer）
1. 为什么使用 Media3 ExoPlayer 而不是其他播放器？它支持 RTMP 是靠什么扩展实现的？
2. 你用到了 media3-datasource-rtmp，说明它的工作原理？
3. ExoPlayer 如何实现低延迟播放？嵌入式场景延迟大概多少毫秒？
4. 播放出现卡顿、花屏、音画不同步，你如何定位并解决？
5. HLS 和 RTMP 同时存在时，切换、容错、降级策略是什么？
6. 多窗口、多画面同时播放多路流，ExoPlayer 如何做实例管理与性能优化？

四、嵌入式与硬件适配（RK 平台）
1. RK 平台上 ExoPlayer 硬件解码是否生效？如何强制开启硬解？
2. 大屏 LED/LCD 显示分辨率高，播放 4K/1080P 时如何避免卡顿、OOM、高 CPU？
3. 预监板卡输出的是什么流？如何接入到 RTMP 服务？
4. 长时间播放（7×24），如何做保活、防卡死、防黑屏、防音视频中断？

五、异常与优化
1. 流媒体项目中最难解决的问题是什么？花屏、延迟、卡顿还是断流？
2. RTMP 流断开后，播放器的自动重连机制如何实现？
3. 弱网环境下，播放器缓冲策略如何调整？
4. Flutter 与原生 ExoPlayer 之间的通信、状态同步、异常回调如何设计？

六、协议与格式深度
1. RTMP 推拉流的基本原理是什么？握手、分块、元数据流程？
2. X264 的 I/B/P 帧分别有什么作用？为什么嵌入式监控场景要提高 I 帧频率？
3. 如何判断当前流是正常、无音频、无视频、码率异常、时间戳异常？